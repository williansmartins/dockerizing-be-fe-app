apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: "mysql-namespace"
  labels:
    app: mysql-label
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080